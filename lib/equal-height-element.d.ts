import React from 'react';
interface Props {
    children?: React.ReactNode;
    name: string;
    placeholder?: boolean;
    disable?: boolean;
    overflow?: boolean;
}
export default function EqualHeightElement(props: Props): JSX.Element;
export {};
